<% categories.each(function(category){ %>
  <%= category.escape('title') %>
  </h3><button class="delete-category" data-category-id="<%= category.escape("id") %>"> Delete Category</button>
  <ul>
  <% category.feeds().each(function(feed){ %>
    <li><%= feed.get('title') %></li>
  <% }) %>
  </ul>
<% }) %>