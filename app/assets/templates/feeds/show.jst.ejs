<% feed.get('entries').forEach(function(entry){ %>
  <div class="entry">
   <h3> <a href="<%= entry.guid %>"><%= entry.title %></a> </h3>
   <p> <%= entry.content %> </p>
   <button class="feed-entry-read" id="<%= entry.id %>">Read? </button>
  </div>
<% }) %>